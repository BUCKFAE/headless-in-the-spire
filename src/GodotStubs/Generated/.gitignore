# Generated by `just regen-godot-stubs` from vendor/sts2.dll's MemberReference
# table. The directory is tracked; the .g.cs files inside are not — they
# regenerate on every build that runs the recipe, and a stale snapshot
# in git would silently drift past the pinned GAME_VERSION.
*.g.cs
