# reward-chain gaps

Generated by `just runner::probe::rewards-natural-chain` (seed=42). Walks from a fresh Ironclad run through the first reachable combat into the post-combat reward set, then drives each reward through the engine's natural claim path — `CardPileCmd.Add(card, PileType.Deck)` for card rewards, `Reward.OnSelectWrapper()` for non-card rewards, `RewardSynchronizer.SyncLocalObtainedCard` for the obtain-listener fan-out — with **no try/catch wrappers**. Each section below records the gaps that surfaced for one reward.

Re-run after every reward-related stub or binding change; the goal is **0 unique gaps** across all sections so Phase 4 can drop the production safety nets without regressing.

## summary

- Rewards cataloged: `3`
- Total unique gaps across all rewards: `0`

## ✓ no gaps — every reward chain runs end-to-end

Phase 4 is safe to proceed: production SelectReward / SkipReward / ClaimCardReward can drop their try/catch wrappers and switch to CardPileCmd.Add without regressing this scenario. Extend the probe (different combats, skippable card rewards, treasure-room relics) to harden coverage further.

## 1. `select-card[2] (cardIndex=0)`

- TerminalState: `ok`
- Synchronous gaps: `0` (0 unique)

✓ no synchronous gaps.

## 2. `select-potion[1]`

- TerminalState: `ok`
- Synchronous gaps: `0` (0 unique)

✓ no synchronous gaps.

## 3. `select-gold[0]`

- TerminalState: `ok`
- Synchronous gaps: `0` (0 unique)

✓ no synchronous gaps.

