# natural-chain gaps

Generated by `just probe-natural-chain` (seed=42). Walks `PlayerCmd.EndTurn` with Player.NetId = LocalContext.NetId = 1uL and no manual side-switch fallback. Each gap below is a `MissingMethodException` / `NullReferenceException` / etc. that the natural chain raised — the Phase-2 punch list.

Re-run after every stub addition; the goal is **0 unique gaps** with TerminalState = `next-player-turn`.

## summary

- TerminalState: `next-player-turn`
- Converged: `True`
- Pump iterations: `1`
- Total exceptions caught (synchronous): `0`
- Unique synchronous gaps: `0`

## ✓ no gaps — natural chain runs end-to-end

Phase 2 is complete for this scenario. Extend the probe to cover reward paths and other combat scenarios before declaring it done.
