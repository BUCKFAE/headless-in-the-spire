# Refactoring Notes

- Create C# Sts2Headless.Utils with
  - `cleanSetupDir` (as in python)
  - `getSts2DLLPath`
  - Other things that are currently duplicated
- Create `Sts2Headless.Commands` as standalone package?
  - The `Program.cs` file is strange, i think there has to be a better way to create a C# cli 
- Improve everything related to coverage
  - New C# Project
  - Wire into `Sts2Headless.Commands`
- Rider complains about namespaces for the `GodotStubs`
  - Does the package need to be `Godot.*` to make the reflection work? Can we supress these arnings?
  - Why are there some things (according to rider) not used (i.e Dictionary?)
- The `Sts2Headless` package contains only very few things (once we are done with cleanup)
- Cleanup `Sts2Headless.Agents`
  - Create sub-folders, one with agent-stuff (like interfaces) and one for the usage (protocol etc)?
- Cleanup `BattleAgent`
  - Is BattleAgent just an implementation of the general Agent thing? How do things relate here?
  - Where should we put these "Bigger" Agents in the future? I am in general not quite sure about the strucuring of the Agents
Cleanup `Replays`
  - How are Replays "linked / used" with agents?
  - I want the replay usage to be basically transparent to the user, it should always just record (and at a good spot)
Cleanup `Sts2Headless.Runtime`
  - Looks like many things in one flat thing, maybe we should split it up into smaller packages?
Unittests
  - Tests reference actual cards (IP of sts2), maybe create dummy test-cards?
End2EndTests
  - Can and should probably be split up into packages